# Shell Command Execution Simulation in C

 This is a C program that simulates piped execution of UNIX ```man``` and ```grep``` commands on the shell.
 
 Shell command for this code: ```man df | grep -A 1 -we “-h” > output.txt```
